services:
  # The Node.js Public Web Service
  - type: web
    name: acutrader-backend
    env: node
    rootDir: backend
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: ML_SERVICE_URL
        fromService:
          type: pserv
          name: acutrader-ml
          property: hostport
          
  # The Python Private Service (ML)
  - type: pserv
    name: acutrader-ml
    env: python
    rootDir: ml_service
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
