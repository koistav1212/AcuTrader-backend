import numpy as np

def build_feature_tensor(ohlcv, tech, market, news_sent, news_impact, fund):
    # Dummy implementation for structure
    # Should return shape (1, 60, num_features)
    return np.zeros((1, 60, 24))
